{
  "name": "landing-page",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "scss": "sass --watch --no-source-map style.scss:style.css",
    "sync": "browser-sync --serveStatic . --files dist -w",
    "start": "yarn run scss & yarn run sync"
  },
  "dependencies": {},
  "devDependencies": {}
}
